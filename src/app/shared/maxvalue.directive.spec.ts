import { MaxvalueDirective } from './maxvalue.directive';

describe('MaxvalueDirective', () => {
  it('should create an instance', () => {
    const directive = new MaxvalueDirective();
    expect(directive).toBeTruthy();
  });
});
